import styled from '@emotion/styled';
import { Link as MuiLink } from '@mui/material';

const Link = styled(MuiLink)(() => ({
    color: 'white',
    display: 'flex',
    alignItems: 'center',
}));

export default Link;
